# Environment Variables Configuration
# Copy this file to .env and fill in the values
# cp .env.example .env

# MongoDB Configuration
# Connection URI for MongoDB database
# Default: mongodb://localhost:27017/push_db
# For Docker: mongodb://mongodb:27017/push_db
MONGODB_URI=mongodb://localhost:27017/push_db

# Database name
# Default: push_db
DATABASE_NAME=push_db

# VAPID Keys for Web Push Notifications
# These keys are used to authenticate push notification requests
# They will be auto-generated on first run if not provided
# To manually generate: python scripts/init_vapid_keys.py
# To validate: python scripts/validate_vapid_keys.py

# VAPID Public Key (base64 URL-safe encoded)
# This key is shared with clients for subscription
# Example: VAPID_PUBLIC_KEY=your_public_key_here
VAPID_PUBLIC_KEY=

# VAPID Private Key (base64 URL-safe encoded)
# This key must be kept secret and never shared
# Example: VAPID_PRIVATE_KEY=your_private_key_here
VAPID_PRIVATE_KEY=

# VAPID Email (contact email for push service)
# Format: mailto:your-email@example.com
# This is used in VAPID claims for push notifications
# Default: mailto:example@example.com
VAPID_EMAIL=mailto:example@example.com

# Note: If VAPID keys are not set, they will be automatically generated
# on application startup and written to this .env file (if writable).
# For production, it's recommended to set these keys manually or via
# environment variables for better security and control.
